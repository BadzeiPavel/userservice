spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/store?currentSchema=user_schema
    username: postgres
    password: postgres
  liquibase:
    url: jdbc:postgresql://postgres:5432/store?currentSchema=user_schema
    user: postgres
    password: postgres
  data:
    redis:
      host: redis
      port: 6379
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always